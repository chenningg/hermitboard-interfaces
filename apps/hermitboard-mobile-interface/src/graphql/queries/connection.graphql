mutation CreateConnection($input: CreateConnectionInput!) {
  createConnection(input: $input) {
    id
    name
    accessToken
    refreshToken
    source {
      id
      name
      icon
      sourceType {
        id
        value
      }
    }
  }
}
